# SPDX-License-Identifier: MIT
# cargo-fuzz harness (W0-7, §34.8). A standalone workspace: cargo-fuzz requires a
# nightly toolchain + libFuzzer, so it is excluded from the main workspace and
# built on demand by `xtask test --kind fuzz` (or the nightly CI fuzz job).
[package]
name = "topomalloc-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
topo-core = { path = "../crates/topo-core" }

[[bin]]
name = "classify"
path = "fuzz_targets/classify.rs"
test = false
doc = false
bench = false

[[bin]]
name = "ptr_class"
path = "fuzz_targets/ptr_class.rs"
test = false
doc = false
bench = false

[[bin]]
name = "extent"
path = "fuzz_targets/extent.rs"
test = false
doc = false
bench = false

[[bin]]
name = "malloc_api"
path = "fuzz_targets/malloc_api.rs"
test = false
doc = false
bench = false

[[bin]]
name = "arena_api"
path = "fuzz_targets/arena_api.rs"
test = false
doc = false
bench = false

[[bin]]
name = "extent_hooks"
path = "fuzz_targets/extent_hooks.rs"
test = false
doc = false
bench = false

# Standalone workspace so cargo-fuzz does not inherit the parent workspace.
[workspace]
