# SPDX-License-Identifier: MIT
[package]
name = "topo-control"
description = "Configuration sources + the control namespace (Appendix E). no_std + alloc."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
license = "MIT"

[dependencies]
topo-core = { path = "../topo-core" }
topo-stats = { path = "../topo-stats" }

[lints]
workspace = true
