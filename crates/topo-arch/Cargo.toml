# SPDX-License-Identifier: MIT
[package]
name = "topo-arch"
description = "Per-architecture support: arch identity and the per-CPU fast-path mode (RSEQ asm lands in plan 05). no_std."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
license = "MIT"

[features]
default = []
# Enables the self-registration RSEQ fallback (W7-1), which needs a
# `thread_local!` to store the per-thread area. The primary glibc-area path and
# all of `topo-arch` are otherwise `no_std`. See `rseq::imp_linux`.
std = []

[dependencies]

[lints]
workspace = true
